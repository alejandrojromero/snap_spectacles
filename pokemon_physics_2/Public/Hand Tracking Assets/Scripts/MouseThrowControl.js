// -----JS CODE-----
//script.motion.api.ballReleased()
//script.motion.api.addImpulse();


  print("EXECUTE?")
        
//RESET the ball
//script.motion.api.reset();
//var average_impulse = getAverageImpulse(handPosQueue);        
//var pos = new vec3(0,0,-100)

script.ball.getTransform().setWorldPosition(new vec3(0,0,-100))
pint ("after execution")
///script.motion.api.ballReleased(pos);
//script.motion.api.addImpulse(new vec3(1,1,1));

//print (average_impulse)

//released = false;
//grabbed = false;